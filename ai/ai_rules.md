1. Don't change unrelated files.
2. Focus on the main point that I ask
3. If you really need to change something, tell me first
4. Don't write any database call on the frontend / react
5. Do not use raw SQL unless absolutely necessary
6. Do not use ORM other than Prisma
7. use tanstack query on the frontend
8. use Axios on the frontend
9. avoid using any
10. seperate big components into different files for modularity

# Coding Style

- Use arrow functions
- Use early returns
- Avoid nested if statements
- Prefer const over let
- Always type function return values

# Naming Conventions

Database tables → singular
Prisma models → PascalCase
Variables → camelCase
Constants → UPPER_CASE
API routes → kebab-case
